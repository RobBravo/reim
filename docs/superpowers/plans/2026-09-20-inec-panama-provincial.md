# INEC Panama Provincial Data Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Give REIM its first geographic dimension below the country (`AdministrativeArea`), and use it to ingest three genuinely-annual provincial indicators from INEC Panama.

**Architecture:** One new reference table (`administrative_areas`), seeded the same way `Country`/`Organization` already are — not created at ingestion time. `Observation` gains a nullable FK onto it; `NULL` keeps meaning "national," so every existing row and source is untouched. `/compare` is fixed to filter it out, since a per-country comparison has nothing correct to do with a per-province row. One new connector, following the existing multi-indicator connector shape, supplies the data.

**Tech Stack:** Python 3.12, SQLAlchemy 2.0 (Mapped/mapped_column), Alembic, FastAPI, Pydantic, pytest, PostgreSQL 16.

**Spec:** `docs/superpowers/specs/2026-09-20-inec-panama-provincial-design.md`

## Global Constraints

- The gate, from the repository root, is
  `.venv/bin/ruff check . && .venv/bin/ruff format --check . && .venv/bin/mypy reim apps && .venv/bin/pytest -q`.
  Integration tests need `REIM_TEST_DATABASE_URL=postgresql+psycopg://reim:reim@localhost:55432/reim`
  (start it with `make db-up CONTAINER_ENGINE=podman` if it is not already running); **without it,
  every test marked `pytest.mark.integration` skips silently.**
- No Docker daemon; use `podman`. No `pip` in `.venv`; use `.venv/bin/<tool>`.
- `ruff format` silently rewrites any ` ```python ` block in Markdown that is not a valid standalone
  module. Fence anything else (JSON, shell, YAML samples) as ` ```text `.
- Never write a real password, token, webhook URL, or personal email address into any file. The
  fixtures in this plan are public government statistics with no such data.
- Every migration must pass `.venv/bin/alembic check` (run via `make migrate-check`) — this fails
  loudly on any drift between the ORM models and the migration history, so it is not optional.
- Follow this codebase's existing patterns exactly rather than inventing new ones — every task below
  names the specific file whose shape to copy.
- This plan corrected itself twice during brainstorming and once more while it was being written
  (see the spec's own "Correction" callouts, and Task 2's province-name verification step below).
  If you find a further inconsistency between this plan and the actual code, the code and a fresh
  measurement are the authority — say what you found and what you did about it in your task report.

---

### Task 1: `AdministrativeArea` — the schema

**Files:**
- Modify: `reim/database/models/reference.py`
- Modify: `reim/database/models/observation.py`
- Modify: `reim/database/models/__init__.py`
- Create: `alembic/versions/<autogenerated>_add_administrative_areas.py`
- Test: `tests/unit/test_database_models.py` (new)

**Interfaces:**
- Produces: `AdministrativeArea` ORM class (`id`, `country_id`, `level`, `code`, `name`), importable
  from `reim.database.models`. `Observation.administrative_area_id: uuid.UUID | None`. Extended
  `NATURAL_KEY_COLUMNS` tuple.

- [ ] **Step 1: Add the `AdministrativeArea` model**

In `reim/database/models/reference.py`, add `AdministrativeArea` to the `TYPE_CHECKING` import
block (it is referenced by `Observation`'s relationship type hint in the next step):

```text
if TYPE_CHECKING:
    from reim.database.models.observation import Observation
    from reim.database.models.pipeline import PipelineRun
```

stays as-is (no change needed there — `AdministrativeArea` doesn't need `Observation`'s
`TYPE_CHECKING` import; it's the other direction that needs one, handled in Step 2).

Add the new class directly after `Country` (after its closing `__table_args__` line, currently
`reim/database/models/reference.py:46`):

```text
class AdministrativeArea(UUIDPrimaryKeyMixin, TimestampMixin, Base):
    """A geographic subdivision below the country level.

    REIM's first geography below ``Country``. ``level`` names the kind of
    subdivision ("province" is the only value today); ``code`` is the
    publisher's own identifier, kept verbatim rather than re-derived, so a
    connector's fixture and REIM's stored row are traceably the same thing.
    """

    __tablename__ = "administrative_areas"

    country_id: Mapped[uuid.UUID] = mapped_column(
        UUID(as_uuid=True), ForeignKey("countries.id", ondelete="RESTRICT"), nullable=False
    )
    level: Mapped[str] = mapped_column(String(40), nullable=False)
    code: Mapped[str] = mapped_column(String(20), nullable=False)
    name: Mapped[str] = mapped_column(String(120), nullable=False)

    country: Mapped[Country] = relationship(back_populates="administrative_areas")
    observations: Mapped[list[Observation]] = relationship(back_populates="administrative_area")

    __table_args__ = (
        UniqueConstraint("country_id", "level", "code", name="uq_administrative_area_natural_key"),
        Index("ix_administrative_areas_country_id", "country_id"),
    )
```

This references `Observation` (only inside a type hint, resolved lazily thanks to
`from __future__ import annotations` already at the top of the file) and `UniqueConstraint`, which
is not yet imported. Update the `sqlalchemy` import line:

```text
from sqlalchemy import Boolean, ForeignKey, Index, String, Text, UniqueConstraint
```

Give `Country` the matching relationship. In the `Country` class body, alongside its existing
`organizations`/`data_sources`/`observations` relationships:

```text
    administrative_areas: Mapped[list[AdministrativeArea]] = relationship(back_populates="country")
```

- [ ] **Step 2: Add `Observation.administrative_area_id` and extend the natural key**

In `reim/database/models/observation.py`, add `AdministrativeArea` to the `TYPE_CHECKING` block
(currently line 27-28):

```text
if TYPE_CHECKING:
    from reim.database.models.reference import AdministrativeArea, Country, DataSource, Indicator
```

Extend `NATURAL_KEY_COLUMNS` (currently line 33):

```text
#: Natural key of an observation. A source may publish exactly one value for a
#: given indicator, country, reporting period and administrative area; a
#: second arrival for the same key is either a no-op (identical payload) or a
#: revision. ``administrative_area_id`` is ``NULL`` for every national-level
#: observation, which every observation was until this column existed.
NATURAL_KEY_COLUMNS = (
    "country_id",
    "indicator_id",
    "source_id",
    "period_start",
    "period_end",
    "administrative_area_id",
)
```

Add the column to `Observation`, in the `# -- Dimensions --` block (after `source_id`, currently
ending at line 55):

```text
    administrative_area_id: Mapped[uuid.UUID | None] = mapped_column(
        UUID(as_uuid=True), ForeignKey("administrative_areas.id", ondelete="RESTRICT")
    )
```

Add the relationship, alongside `country`/`indicator`/`source` (currently lines 93-95):

```text
    administrative_area: Mapped[AdministrativeArea | None] = relationship(
        back_populates="observations"
    )
```

Update `__table_args__`'s `UniqueConstraint(*NATURAL_KEY_COLUMNS, ...)` — no code change needed
there, it already unpacks the tuple, so it picks up the new column automatically. Confirm this by
reading `reim/database/models/observation.py:102-103` and checking `NATURAL_KEY_COLUMNS` is what it
unpacks (it is).

- [ ] **Step 3: Export from the package**

In `reim/database/models/__init__.py`, add the import and `__all__` entry:

```text
from reim.database.models.reference import AdministrativeArea, Country, DataSource, Indicator, Organization
```

and add `"AdministrativeArea",` to `__all__` (alphabetically, before `"AlertState"`).

- [ ] **Step 4: Generate and inspect the migration**

```bash
.venv/bin/alembic revision --autogenerate -m "add administrative_areas"
```

Open the generated file under `alembic/versions/`. It must create the `administrative_areas` table
(columns, the unique constraint, the index) and alter `observations` to add
`administrative_area_id` plus **drop and recreate** `uq_observations_natural_key` with the new
column included (Alembic detects this as two operations — a constraint drop and a new one with the
extended column list — not a single ALTER; if it emits something different, or misses the FK
constraint on the new column, fix the generated file by hand rather than editing the models to
match a wrong migration). Compare the generated `upgrade()`/`downgrade()` against
`alembic/versions/f4d56214628a_add_api_keys.py` for the general shape (table creation) — this one
additionally needs `op.add_column` and constraint operations on the existing `observations` table.

- [ ] **Step 5: Apply it and check for drift**

```bash
.venv/bin/alembic upgrade head
.venv/bin/alembic check
```

Expected: both succeed with no output (silence is the pass). If `alembic check` reports drift, the
migration is missing something the models declare — fix the migration file, not the models.

- [ ] **Step 6: Write a model round-trip test**

Create `tests/unit/test_database_models.py`:

```text
"""Model-level checks that don't need seeded reference data."""

from __future__ import annotations

from reim.database.models import NATURAL_KEY_COLUMNS, AdministrativeArea, Observation


def test_natural_key_columns_includes_administrative_area() -> None:
    assert NATURAL_KEY_COLUMNS == (
        "country_id",
        "indicator_id",
        "source_id",
        "period_start",
        "period_end",
        "administrative_area_id",
    )


def test_administrative_area_id_is_nullable_on_observation() -> None:
    column = Observation.__table__.columns["administrative_area_id"]
    assert column.nullable is True


def test_administrative_area_table_has_the_expected_columns() -> None:
    columns = set(AdministrativeArea.__table__.columns.keys())
    assert columns == {"id", "country_id", "level", "code", "name", "created_at", "updated_at"}
```

- [ ] **Step 7: Run it**

```bash
.venv/bin/pytest tests/unit/test_database_models.py -v
```

Expected: 3 passed.

- [ ] **Step 8: Gate and commit**

```bash
.venv/bin/ruff check . && .venv/bin/ruff format --check . && .venv/bin/mypy reim apps
REIM_TEST_DATABASE_URL=postgresql+psycopg://reim:reim@localhost:55432/reim .venv/bin/pytest -q
```

```bash
git add reim/database/models/reference.py reim/database/models/observation.py \
  reim/database/models/__init__.py alembic/versions/*_add_administrative_areas.py \
  tests/unit/test_database_models.py
git commit -m "feat(db): add administrative_areas, REIM's first geography below Country"
```

---

### Task 2: The domain layer — geography registry, `NormalizedObservation`, hashing

**Files:**
- Create: `reim/domain/geography/__init__.py`
- Create: `reim/domain/geography/registry.py`
- Modify: `reim/domain/pipelines/models.py`
- Modify: `reim/domain/observations/hashing.py`
- Modify: `tests/conftest.py` (`make_observation` factory)
- Test: `tests/unit/test_geography_registry.py` (new)
- Test: `tests/unit/test_hashing.py` (existing — extend)

**Interfaces:**
- Consumes: nothing from Task 1 at runtime (this task's tests don't touch the database).
- Produces: `AdministrativeAreaDefinition` dataclass, `PANAMA_PROVINCES: tuple[AdministrativeAreaDefinition, ...]`
  (10 entries) in `reim.domain.geography.registry`. `NormalizedObservation.administrative_area_code: str | None`.
  Extended `natural_key` tuple shape: `tuple[str, str, str, str, str, str]` (6 elements, was 5) — Task 3
  and Task 6 both construct `NormalizedObservation` with this field.

- [ ] **Step 1: Verify the province code→name mapping before writing it down**

This plan's spec (`docs/superpowers/specs/2026-09-20-inec-panama-provincial-design.md`, §2.2,
decision D7) deliberately left this mapping for implementation. The candidate mapping below is a
research best-effort from public knowledge of Panama's administrative-division numbering, **not a
live-fetched fact** — verify it before seeding it, the same "never fabricate data" discipline
`docs/sources.md`'s rules for adding a source require of every connector.

Candidate mapping (INEC's own province codes, which are `"01"`–`"09"` then `"13"` — codes `10`–`12`
are Panama's three indigenous comarcas, a different administrative category from a province, which
is why they don't appear in a `nivel_geografico=Provincia` response):

| code | name |
|---|---|
| 01 | Bocas del Toro |
| 02 | Coclé |
| 03 | Colón |
| 04 | Chiriquí |
| 05 | Darién |
| 06 | Herrera |
| 07 | Los Santos |
| 08 | Panamá |
| 09 | Veraguas |
| 13 | Panamá Oeste |

Verify it with this reproducible cross-check before trusting it: fetch id 232 (automobiles per
1000 inhabitants) for 2023 —

```bash
curl -s -A "REIM-verify/1.0" \
  "https://www.inec.gob.pa/m_2/api/data/choropleth?nivel_geografico=Provincia&id_variable=232&anio=2023" \
  | python3 -m json.tool
```

— and confirm two extremes that are independently well-known, public facts about Panama's
geography: code `"05"` should carry the **lowest** value of the ten (Darién has no paved road
connecting it to the rest of the country — the Darién Gap — so near-zero vehicle registration is
expected), and code `"08"` should carry the **highest** value by a wide margin (Panamá province
contains Panama City, the country's overwhelmingly dominant urban and economic center). If both
hold — they did on 2026-09-20 (`"05"` → `1.8`, the minimum; `"08"` → `395.4`, the maximum by more
than double the second-highest) — the mapping is corroborated at its two most identifiable points.
If either does not hold when you check, do not proceed on the candidate mapping: search for INEC's
own published "Codificación de la División Político-Administrativa" (or equivalent official
province-code table) and use that instead, and say so in your task report.

- [ ] **Step 2: Write the geography registry**

Create `reim/domain/geography/__init__.py` (empty — matches `reim/domain/countries/__init__.py`).

Create `reim/domain/geography/registry.py`, following `reim/domain/countries/registry.py`'s shape
exactly:

```text
"""Canonical administrative-area definitions below the country level.

REIM's first geography below ``Country``. Only Panama's provinces exist today
— INEC Panama is the only subnational source REIM reads — but nothing here is
Panama-specific: a second country's subnational source would add its own
tuple of definitions, not change this shape.
"""

from __future__ import annotations

from dataclasses import dataclass


@dataclass(frozen=True, slots=True)
class AdministrativeAreaDefinition:
    """Static description of a subdivision below one country."""

    country_iso2: str
    level: str
    code: str
    name: str


#: Panama's ten provinces, as INEC's own ``nivel_geografico=Provincia``
#: choropleth endpoint codes them. Codes "10"-"12" are the three indigenous
#: comarcas, a different administrative category, and never appear in a
#: ``Provincia``-level response.
PANAMA_PROVINCES: tuple[AdministrativeAreaDefinition, ...] = (
    AdministrativeAreaDefinition(country_iso2="PA", level="province", code="01", name="Bocas del Toro"),
    AdministrativeAreaDefinition(country_iso2="PA", level="province", code="02", name="Coclé"),
    AdministrativeAreaDefinition(country_iso2="PA", level="province", code="03", name="Colón"),
    AdministrativeAreaDefinition(country_iso2="PA", level="province", code="04", name="Chiriquí"),
    AdministrativeAreaDefinition(country_iso2="PA", level="province", code="05", name="Darién"),
    AdministrativeAreaDefinition(country_iso2="PA", level="province", code="06", name="Herrera"),
    AdministrativeAreaDefinition(country_iso2="PA", level="province", code="07", name="Los Santos"),
    AdministrativeAreaDefinition(country_iso2="PA", level="province", code="08", name="Panamá"),
    AdministrativeAreaDefinition(country_iso2="PA", level="province", code="09", name="Veraguas"),
    AdministrativeAreaDefinition(country_iso2="PA", level="province", code="13", name="Panamá Oeste"),
)

#: Every registered administrative area, source of truth for seeding.
ADMINISTRATIVE_AREAS: tuple[AdministrativeAreaDefinition, ...] = PANAMA_PROVINCES
```

If Step 1's verification led you to a different mapping, use that instead of the table above — the
code shape (the dataclass, the tuple, the module layout) does not change either way.

- [ ] **Step 3: Write the failing test for the registry**

Create `tests/unit/test_geography_registry.py`:

```text
"""Structural checks on the administrative-area registry."""

from __future__ import annotations

from reim.domain.geography.registry import ADMINISTRATIVE_AREAS, PANAMA_PROVINCES


def test_panama_has_exactly_ten_provinces() -> None:
    assert len(PANAMA_PROVINCES) == 10


def test_every_province_code_is_unique() -> None:
    codes = [p.code for p in PANAMA_PROVINCES]
    assert len(codes) == len(set(codes))


def test_every_province_belongs_to_panama_at_province_level() -> None:
    for province in PANAMA_PROVINCES:
        assert province.country_iso2 == "PA"
        assert province.level == "province"


def test_administrative_areas_is_the_registry_of_record() -> None:
    assert ADMINISTRATIVE_AREAS == PANAMA_PROVINCES
```

- [ ] **Step 4: Run it**

```bash
.venv/bin/pytest tests/unit/test_geography_registry.py -v
```

Expected: 4 passed.

- [ ] **Step 5: Extend `NormalizedObservation` and `natural_key`**

In `reim/domain/pipelines/models.py`, add one field to `NormalizedObservation` (after
`source_record_id`, before `raw_metadata`, currently lines 66-67):

```text
    source_record_id: str | None = None
    #: Publisher's own code for a subnational area (e.g. INEC Panama's
    #: ``id_provincia``). ``None`` for every national-level observation.
    administrative_area_code: str | None = None
    raw_metadata: dict[str, Any] = field(default_factory=dict)
```

Extend `natural_key` (currently lines 83-92) to include it — the ordering matters: this must go
last, after `period_end`, to keep the existing five-element tuples in every current test failing
loudly (a type error, not a silent wrong-shape pass) rather than quietly matching a stale
expectation:

```text
    @property
    def natural_key(self) -> tuple[str, str, str, str, str, str]:
        """Return the natural key tuple identifying this datapoint.

        The sixth element distinguishes a provincial observation from the
        national one for the same indicator and period — without it, ten
        provincial rows sharing a country/indicator/source/period would look
        like ten duplicates of the same key to
        ``reim.domain.quality.checks``' in-batch duplicate check.
        """
        return (
            self.country_iso3.upper(),
            self.indicator_code.lower(),
            self.source_key.lower(),
            self.period.start.isoformat(),
            self.period.end.isoformat(),
            (self.administrative_area_code or "").upper(),
        )
```

`compute_content_hash` needs **no change** — it is compared only against a single
already-identified row (Task 3 extends the lookup that identifies it), not used to distinguish
rows itself.

- [ ] **Step 6: Extend the module-level `natural_key()` in hashing.py**

In `reim/domain/observations/hashing.py`, extend `natural_key()` (currently lines 36-52) to match:

```text
def natural_key(
    *,
    country_iso3: str,
    indicator_code: str,
    source_key: str,
    period_start: date,
    period_end: date,
    administrative_area_code: str | None = None,
) -> tuple[str, str, str, str, str, str]:
    """Return the tuple that uniquely identifies an observation."""
    return (
        country_iso3.upper(),
        indicator_code.lower(),
        source_key.lower(),
        period_start.isoformat(),
        period_end.isoformat(),
        (administrative_area_code or "").upper(),
    )
```

`content_hash()` (the function, currently lines 65-96) needs **no change**, for the same reason as
`compute_content_hash` above — keep the two natural-key implementations in sync, but leave
content_hash alone.

- [ ] **Step 7: Extend the existing hashing tests**

Open `tests/unit/test_hashing.py`. Find the test(s) that call `natural_key(...)` directly and
confirm its return shape — extend at least one to assert the new tuple length and the default:

```text
def test_natural_key_defaults_administrative_area_to_empty() -> None:
    from reim.domain.observations.hashing import natural_key
    from datetime import date

    key = natural_key(
        country_iso3="PAN",
        indicator_code="pa_test",
        source_key="test_source",
        period_start=date(2023, 1, 1),
        period_end=date(2023, 12, 31),
    )
    assert len(key) == 6
    assert key[-1] == ""


def test_natural_key_distinguishes_administrative_areas() -> None:
    from reim.domain.observations.hashing import natural_key
    from datetime import date

    kwargs = dict(
        country_iso3="PAN",
        indicator_code="pa_test",
        source_key="test_source",
        period_start=date(2023, 1, 1),
        period_end=date(2023, 12, 31),
    )
    national = natural_key(**kwargs)
    provincial = natural_key(**kwargs, administrative_area_code="08")
    assert national != provincial
```

(Place these near the file's other `natural_key` tests rather than at the end, matching the
file's existing organization — read it first to find where.)

- [ ] **Step 8: Extend `make_observation` in `tests/conftest.py`**

`tests/integration/test_persistence.py` and other integration tests build `NormalizedObservation`
via the `make_observation` fixture (`tests/conftest.py:301-329`). Add the new field so Task 3's
tests can use it:

```text
    def _build(
        period: str = "2024",
        value: str | Decimal | None = "10.5",
        *,
        indicator_code: str = "ni_cpi_inflation_annual",
        source_key: str = "worldbank_ni_cpi_inflation",
        country_iso3: str = "NIC",
        unit: str = "percent",
        currency_code: str | None = None,
        frequency: Frequency | None = None,
        value_text: str | None = None,
        retrieved_at: datetime | None = None,
        administrative_area_code: str | None = None,
    ) -> NormalizedObservation:
        return NormalizedObservation(
            country_iso3=country_iso3,
            indicator_code=indicator_code,
            source_key=source_key,
            period=parse_period(period, frequency),
            unit=unit,
            currency_code=currency_code,
            value_numeric=None if value is None else Decimal(str(value)),
            value_text=value_text,
            retrieved_at=retrieved_at or datetime(2026, 8, 4, 12, 0, tzinfo=UTC),
            source_url="https://api.worldbank.org/v2/country/NIC/indicator/FP.CPI.TOTL.ZG",
            source_record_id=f"FP.CPI.TOTL.ZG:{period}",
            administrative_area_code=administrative_area_code,
        )
```

- [ ] **Step 9: Run the full unit suite for this layer**

```bash
.venv/bin/pytest tests/unit/test_geography_registry.py tests/unit/test_hashing.py \
  tests/unit/test_database_models.py -v
```

Expected: all pass, no failures, no collection errors (confirms `conftest.py`'s edit didn't break
anything importing it).

- [ ] **Step 10: Gate and commit**

```bash
.venv/bin/ruff check . && .venv/bin/ruff format --check . && .venv/bin/mypy reim apps
REIM_TEST_DATABASE_URL=postgresql+psycopg://reim:reim@localhost:55432/reim .venv/bin/pytest -q
```

```bash
git add reim/domain/geography reim/domain/pipelines/models.py \
  reim/domain/observations/hashing.py tests/conftest.py \
  tests/unit/test_geography_registry.py tests/unit/test_hashing.py
git commit -m "feat(domain): province registry, and administrative_area in the natural key"
```

---

### Task 3: Persistence — seeding, lookup, and the writer

**Files:**
- Modify: `reim/repositories/reference.py`
- Modify: `reim/repositories/observations.py`
- Modify: `reim/services/seeding.py`
- Modify: `reim/services/observation_writer.py`
- Test: `tests/integration/test_persistence.py` (existing — extend)

**Interfaces:**
- Consumes: `AdministrativeArea` (Task 1), `PANAMA_PROVINCES`/`ADMINISTRATIVE_AREAS` (Task 2),
  `NormalizedObservation.administrative_area_code` (Task 2).
- Produces: `require_administrative_area_by_code(session, country_id, level, code) -> AdministrativeArea`
  in `reim.repositories.reference`. `seed_administrative_areas(session, report)` in
  `reim.services.seeding`, called from `seed_all`. `write_observations` now persists
  `administrative_area_id` and raises `UnknownReferenceError` for an unregistered code.

- [ ] **Step 1: Add the lookup functions**

In `reim/repositories/reference.py`, import `AdministrativeArea`:

```text
from reim.database.models import AdministrativeArea, Country, DataSource, Indicator, Organization
```

Add, following `require_country_by_iso3`'s exact shape (after the existing organization lookups at
the end of the file):

```text
def get_administrative_area_by_code(
    session: Session, country_id: uuid.UUID, level: str, code: str
) -> AdministrativeArea | None:
    """Return the administrative area with this code, if it exists."""
    return session.scalar(
        select(AdministrativeArea).where(
            AdministrativeArea.country_id == country_id,
            AdministrativeArea.level == level,
            AdministrativeArea.code == code,
        )
    )


def require_administrative_area_by_code(
    session: Session, country_id: uuid.UUID, level: str, code: str
) -> AdministrativeArea:
    """Return the administrative area or raise :class:`UnknownReferenceError`."""
    area = get_administrative_area_by_code(session, country_id, level, code)
    if area is None:
        msg = f"Administrative area {level}:{code!r} is not registered; run 'reim db seed' first"
        raise UnknownReferenceError(msg, administrative_area_code=code)
    return area
```

- [ ] **Step 2: Extend `get_by_natural_key`**

In `reim/repositories/observations.py`, `get_by_natural_key` (currently lines 168-186):

```text
def get_by_natural_key(
    session: Session,
    *,
    country_id: uuid.UUID,
    indicator_id: uuid.UUID,
    source_id: uuid.UUID,
    period_start: date,
    period_end: date,
    administrative_area_id: uuid.UUID | None = None,
) -> Observation | None:
    """Return the observation matching the natural key, if it exists."""
    return session.scalar(
        select(Observation).where(
            Observation.country_id == country_id,
            Observation.indicator_id == indicator_id,
            Observation.source_id == source_id,
            Observation.period_start == period_start,
            Observation.period_end == period_end,
            Observation.administrative_area_id == administrative_area_id,
        )
    )
```

`Observation.administrative_area_id == administrative_area_id` is deliberately `==`, not
`.is_(administrative_area_id)` — SQLAlchemy's `==` operator already compiles to `IS NULL` when the
right-hand side is Python `None`, so this one line is correct for both the "national" and the
"provincial" case.

- [ ] **Step 3: Seed the registry**

In `reim/services/seeding.py`, import the registry and model:

```text
from reim.database.models import AdministrativeArea, Country, DataSource, Indicator, Organization
from reim.domain.geography.registry import ADMINISTRATIVE_AREAS
```

Add `administrative_areas_created`/`administrative_areas_updated` to `SeedReport`, alongside the
existing counters, and fold them into `total_created`/`total_updated`:

```text
@dataclass(slots=True)
class SeedReport:
    """Counts of what seeding created and updated."""

    countries_created: int = 0
    countries_updated: int = 0
    organizations_created: int = 0
    organizations_updated: int = 0
    administrative_areas_created: int = 0
    administrative_areas_updated: int = 0
    indicators_created: int = 0
    indicators_updated: int = 0
    sources_created: int = 0
    sources_updated: int = 0

    @property
    def total_created(self) -> int:
        """Rows inserted across all reference tables."""
        return (
            self.countries_created
            + self.organizations_created
            + self.administrative_areas_created
            + self.indicators_created
            + self.sources_created
        )

    @property
    def total_updated(self) -> int:
        """Rows modified across all reference tables."""
        return (
            self.countries_updated
            + self.organizations_updated
            + self.administrative_areas_updated
            + self.indicators_updated
            + self.sources_updated
        )
```

Add `seed_administrative_areas`, following `seed_organizations`'s shape exactly (a country lookup,
then a definition loop):

```text
def seed_administrative_areas(session: Session, report: SeedReport) -> None:
    """Insert or refresh every administrative area in the registry."""
    countries = {country.iso2: country for country in session.scalars(select(Country))}
    existing = {
        (area.country_id, area.level, area.code): area
        for area in session.scalars(select(AdministrativeArea))
    }

    for definition in ADMINISTRATIVE_AREAS:
        country = countries.get(definition.country_iso2)
        if country is None:
            msg = (
                f"Administrative area {definition.code} references unseeded country "
                f"{definition.country_iso2}"
            )
            raise UnknownReferenceError(msg, administrative_area_code=definition.code)

        values = {"name": definition.name}
        key = (country.id, definition.level, definition.code)
        current = existing.get(key)
        if current is None:
            session.add(
                AdministrativeArea(
                    country_id=country.id, level=definition.level, code=definition.code, **values
                )
            )
            report.administrative_areas_created += 1
        elif _sync(current, values):
            report.administrative_areas_updated += 1
    session.flush()
```

Call it from `seed_all`, **before** `seed_indicators` (order doesn't matter functionally here since
neither depends on the other, but grouping it with the other "static, code-defined" reference data
before the catalog-derived `seed_sources` keeps the dependency order readable):

```text
def seed_all(session: Session, catalog: SourceCatalog | None = None) -> SeedReport:
    """Seed every reference table, in dependency order.

    Args:
        session: Active session; the caller owns the transaction.
        catalog: Catalog to materialise. Defaults to the process-wide catalog.
    """
    report = SeedReport()
    seed_countries(session, report)
    seed_organizations(session, report)
    seed_administrative_areas(session, report)
    seed_indicators(session, report)
    seed_sources(session, report, catalog or get_catalog())
    logger.info(
        "seed.completed",
        created=report.total_created,
        updated=report.total_updated,
    )
    return report
```

- [ ] **Step 4: Extend the writer**

In `reim/services/observation_writer.py`, import `AdministrativeArea` and the new repository
function:

```text
from reim.database.models import (
    AdministrativeArea,
    Country,
    DataSource,
    Indicator,
    Observation,
    ObservationRevision,
)
from reim.repositories.reference import (
    require_administrative_area_by_code,
    require_country_by_iso3,
    require_indicator_by_code,
    require_source_by_key,
)
```

Extend `_ReferenceCache` (currently lines 74-98) with an area cache and lookup, following the
`indicator()`/`source()` pattern exactly — note this one needs the resolved `country_id`, not just
a code, so it takes the already-resolved `Country` rather than looking one up itself:

```text
@dataclass(slots=True)
class _ReferenceCache:
    """Per-batch cache so reference lookups do not hit the DB once per row."""

    countries: dict[str, Country] = field(default_factory=dict)
    indicators: dict[str, Indicator] = field(default_factory=dict)
    sources: dict[str, DataSource] = field(default_factory=dict)
    administrative_areas: dict[tuple[uuid.UUID, str], AdministrativeArea] = field(
        default_factory=dict
    )

    def country(self, session: Session, iso3: str) -> Country:
        """Return (and memoise) the country for ``iso3``."""
        if iso3 not in self.countries:
            self.countries[iso3] = require_country_by_iso3(session, iso3)
        return self.countries[iso3]

    def indicator(self, session: Session, code: str) -> Indicator:
        """Return (and memoise) the indicator for ``code``."""
        if code not in self.indicators:
            self.indicators[code] = require_indicator_by_code(session, code)
        return self.indicators[code]

    def source(self, session: Session, key: str) -> DataSource:
        """Return (and memoise) the data source for ``key``."""
        if key not in self.sources:
            self.sources[key] = require_source_by_key(session, key)
        return self.sources[key]

    def administrative_area(
        self, session: Session, country_id: uuid.UUID, code: str
    ) -> AdministrativeArea:
        """Return (and memoise) the administrative area for ``code`` within a country.

        ``level`` is hardcoded to ``"province"`` here because that is the only
        level any connector produces today — widen this when a second level
        exists.
        """
        key = (country_id, code)
        if key not in self.administrative_areas:
            self.administrative_areas[key] = require_administrative_area_by_code(
                session, country_id, "province", code
            )
        return self.administrative_areas[key]
```

In `write_observations`, resolve the area alongside country/indicator/source (currently lines
140-142):

```text
        country = cache.country(session, incoming.country_iso3)
        indicator = cache.indicator(session, incoming.indicator_code)
        source = cache.source(session, incoming.source_key)
        administrative_area = (
            cache.administrative_area(session, country.id, incoming.administrative_area_code)
            if incoming.administrative_area_code is not None
            else None
        )
        digest = incoming.compute_content_hash()

        existing = get_by_natural_key(
            session,
            country_id=country.id,
            indicator_id=indicator.id,
            source_id=source.id,
            period_start=incoming.period.start,
            period_end=incoming.period.end,
            administrative_area_id=administrative_area.id if administrative_area else None,
        )
```

Pass it through to `_build_observation` (currently called at line 156-167):

```text
        if existing is None:
            session.add(
                _build_observation(
                    incoming,
                    country=country,
                    indicator=indicator,
                    source=source,
                    administrative_area=administrative_area,
                    content_hash=digest,
                    validation_status=status,
                    connector_version=connector_version,
                    pipeline_version=pipeline_version,
                )
            )
            report.record(WriteOutcome.INSERTED)
            continue
```

Update `_build_observation`'s signature and body (currently lines 207-240):

```text
def _build_observation(
    incoming: NormalizedObservation,
    *,
    country: Country,
    indicator: Indicator,
    source: DataSource,
    administrative_area: AdministrativeArea | None,
    content_hash: str,
    validation_status: ValidationStatus,
    connector_version: str,
    pipeline_version: str,
) -> Observation:
    return Observation(
        country_id=country.id,
        indicator_id=indicator.id,
        source_id=source.id,
        administrative_area_id=administrative_area.id if administrative_area else None,
        period_start=incoming.period.start,
        period_end=incoming.period.end,
        period_label=incoming.period.label,
        value_numeric=incoming.value_numeric,
        value_text=incoming.value_text,
        unit=incoming.unit,
        currency_code=incoming.currency_code,
        published_at=incoming.published_at,
        retrieved_at=incoming.retrieved_at,
        source_url=incoming.source_url,
        source_record_id=incoming.source_record_id,
        content_hash=content_hash,
        status=ObservationStatus.ACTIVE,
        validation_status=validation_status,
        revision_count=0,
        connector_version=connector_version,
        pipeline_version=pipeline_version,
        raw_metadata=incoming.raw_metadata,
    )
```

(`_apply_revision`, the update path for an existing row, needs **no change** —
`administrative_area_id` is part of the natural key, so a revision to an existing row can never
change which area it belongs to; only its value/unit/etc. can change, and those fields are already
covered.)

No new `import uuid` is needed — `observation_writer.py:18` already imports it for
`pipeline_run_id`, and `_ReferenceCache.administrative_areas`'s dict key type reuses it.

- [ ] **Step 5: Write the failing integration tests**

In `tests/integration/test_persistence.py`, add near the other reference-data tests (after
`test_disabled_source_is_marked_inactive`, before the observation-insertion tests):

```text
def test_seed_creates_administrative_areas(seeded_session: Session) -> None:
    from reim.database.models import AdministrativeArea

    assert seeded_session.scalar(select(func.count(AdministrativeArea.id))) == 10


def test_administrative_area_seeding_is_idempotent(seeded_session: Session, catalog) -> None:  # type: ignore[no-untyped-def]
    from reim.database.models import AdministrativeArea

    before = seeded_session.scalar(select(func.count(AdministrativeArea.id)))
    report = seed_all(seeded_session, catalog)
    assert report.administrative_areas_created == 0
    assert seeded_session.scalar(select(func.count(AdministrativeArea.id))) == before
```

Add near the other persistence/idempotency tests (this is the load-bearing new coverage —
national and provincial observations for the same indicator/period must coexist as different
rows, and a duplicate provincial observation must not double-insert):

```text
def test_a_national_and_a_provincial_observation_coexist(
    seeded_session: Session, make_observation
) -> None:  # type: ignore[no-untyped-def]
    """administrative_area_id NULL and a real value are different rows for the same key."""
    national = make_observation(
        "2023", "205.1", country_iso3="PAN", indicator_code="ni_cpi_inflation_annual"
    )
    provincial = make_observation(
        "2023",
        "9.6",
        country_iso3="PAN",
        indicator_code="ni_cpi_inflation_annual",
        administrative_area_code="01",
    )
    report = _write(seeded_session, [national, provincial])
    assert (report.inserted, report.updated, report.unchanged) == (2, 0, 0)
    assert _count(seeded_session) == 2


def test_rerunning_a_provincial_batch_inserts_nothing_the_second_time(
    seeded_session: Session, make_observation
) -> None:  # type: ignore[no-untyped-def]
    batch = [
        make_observation(
            "2023",
            "9.6",
            country_iso3="PAN",
            indicator_code="ni_cpi_inflation_annual",
            administrative_area_code="01",
        )
    ]
    _write(seeded_session, batch)
    second = _write(seeded_session, batch)
    assert (second.inserted, second.updated, second.unchanged) == (0, 0, 1)
    assert _count(seeded_session) == 1


def test_an_unregistered_administrative_area_code_raises(
    seeded_session: Session, make_observation
) -> None:  # type: ignore[no-untyped-def]
    """A code the registry doesn't have must fail loudly, not mint a new area."""
    with pytest.raises(UnknownReferenceError, match="not registered"):
        _write(
            seeded_session,
            [
                make_observation(
                    "2023",
                    "1",
                    country_iso3="PAN",
                    indicator_code="ni_cpi_inflation_annual",
                    administrative_area_code="99",
                )
            ],
        )
```

`make_observation`'s default `indicator_code`/`source_key` are Nicaragua-shaped
(`ni_cpi_inflation_annual`/`worldbank_ni_cpi_inflation`); these tests override `country_iso3="PAN"`
but keep that indicator code deliberately — the natural key doesn't care that a Nicaragua-registered
indicator is being used for a Panama-country observation, and reusing an already-seeded indicator
keeps these tests from depending on Task 6's not-yet-written Panama indicators. This mirrors how
`test_tiny_values_survive_the_round_trip` (line 138) already overrides `indicator_code` and
`source_key` independently of `country_iso3` for the same reason.

- [ ] **Step 6: Run them to verify they fail first**

```bash
REIM_TEST_DATABASE_URL=postgresql+psycopg://reim:reim@localhost:55432/reim \
  .venv/bin/pytest tests/integration/test_persistence.py -k "administrative_area or coexist or unregistered" -v
```

Expected: every one of the six new tests fails — `test_seed_creates_administrative_areas` on the
count (0 seeded, not 10), the coexist/rerun tests likely with a `TypeError` (`make_observation`
doesn't accept `administrative_area_code` until Task 2's Step 8, which should already be done —
if these still fail with that `TypeError`, Task 2 wasn't actually merged in; check before
proceeding), and the unregistered-code test failing because nothing raises yet.

- [ ] **Step 7: Implement, then run again to verify they pass**

Steps 1-4 above are the implementation. Re-run the same command from Step 6.

Expected: 6 passed.

- [ ] **Step 8: Run the full persistence suite, to confirm no regression**

```bash
REIM_TEST_DATABASE_URL=postgresql+psycopg://reim:reim@localhost:55432/reim \
  .venv/bin/pytest tests/integration/test_persistence.py -v
```

Expected: every existing test still passes alongside the six new ones.

- [ ] **Step 9: Gate and commit**

```bash
.venv/bin/ruff check . && .venv/bin/ruff format --check . && .venv/bin/mypy reim apps
REIM_TEST_DATABASE_URL=postgresql+psycopg://reim:reim@localhost:55432/reim .venv/bin/pytest -q
```

```bash
git add reim/repositories/reference.py reim/repositories/observations.py \
  reim/services/seeding.py reim/services/observation_writer.py \
  tests/integration/test_persistence.py
git commit -m "feat(persistence): seed, resolve and write against administrative_area"
```

---

### Task 4: `/compare` must not see subnational rows

**Files:**
- Modify: `reim/repositories/comparison.py`
- Test: `tests/integration/test_comparison_repository.py` (existing — extend)

**Interfaces:**
- Consumes: `Observation.administrative_area_id` (Task 1), the writer's ability to persist a
  provincial observation (Task 3).

- [ ] **Step 1: Write the failing guard test**

Open `tests/integration/test_comparison_repository.py` and read it to find an existing test that
exercises `fetch_comparison_cells` (or `count_comparison_periods`) against a known indicator/country
pair with a predictable cell count — copy its setup pattern rather than inventing a new one. Add:

```text
def test_a_provincial_observation_is_invisible_to_compare(
    session: Session, make_observation
) -> None:  # type: ignore[no-untyped-def]
    """/compare has no correct behaviour for a per-province row; it must not see one.

    Plants a provincial observation for an indicator/country pair /compare
    already reads, and confirms the comparison result is identical to what it
    would be without that row — same cell count, same value, no error.
    """
    from reim.repositories.comparison import ComparisonQuery, count_comparison_periods
    from reim.repositories.reference import get_country_by_iso3
    from reim.services.observation_writer import write_observations

    panama = get_country_by_iso3(session, "PAN")
    assert panama is not None

    national = make_observation(
        "2023", "205.1", country_iso3="PAN", indicator_code="ni_cpi_inflation_annual"
    )
    write_observations(session, [national], connector_version="1.0.0")
    session.flush()

    query = ComparisonQuery(
        indicator_code="ni_cpi_inflation_annual",
        country_ids=(panama.id,),
        period_start_from=None,
        period_start_to=None,
    )
    before = count_comparison_periods(session, query)

    provincial = make_observation(
        "2023",
        "9.6",
        country_iso3="PAN",
        indicator_code="ni_cpi_inflation_annual",
        administrative_area_code="01",
    )
    write_observations(session, [provincial], connector_version="1.0.0")
    session.flush()

    after = count_comparison_periods(session, query)
    assert after == before
```

`ComparisonQuery`'s fields (`reim/repositories/comparison.py:27-33`) were confirmed while writing
this plan: `indicator_code: str`, `country_ids: tuple[uuid.UUID, ...]`,
`period_start_from: date | None = None`, `period_start_to: date | None = None` — exactly what the
test above constructs.

- [ ] **Step 2: Run it to verify it fails**

```bash
REIM_TEST_DATABASE_URL=postgresql+psycopg://reim:reim@localhost:55432/reim \
  .venv/bin/pytest tests/integration/test_comparison_repository.py -k provincial -v
```

Expected: FAIL — `after != before` (the provincial row's period gets counted a second time, or the
query errors, depending on exactly how `_restrict`'s current output combines with the extra row).

- [ ] **Step 3: Fix `_restrict`**

In `reim/repositories/comparison.py`, `_restrict` (currently lines 75-86):

```text
def _restrict[S: Select[Any]](statement: S, query: ComparisonQuery) -> S:
    """Narrow a statement already joined to indicator and country."""
    statement = statement.where(
        Indicator.code == query.indicator_code,
        Observation.country_id.in_(query.country_ids),
        Observation.status == ObservationStatus.ACTIVE,
        Observation.administrative_area_id.is_(None),
    )
    if query.period_start_from is not None:
        statement = statement.where(Observation.period_start >= query.period_start_from)
    if query.period_start_to is not None:
        statement = statement.where(Observation.period_start <= query.period_start_to)
    return statement
```

This one line fixes `_period_page`, `count_comparison_periods`, `fetch_comparison_cells` and
`summarise_series` at once — every one of them calls `_restrict`.

`fetch_rates` (currently lines 237-276) builds its own `where(...)` clause directly rather than
calling `_restrict` — for the same defensive reason (every `Observation` read in this file should
carry the same scope, even though `RATE_INDICATOR_CODE` is a fixed national exchange-rate indicator
that will not realistically gain subnational data), add the same condition to its `where(...)` call
(currently lines 261-267):

```text
        .where(
            Indicator.code == RATE_INDICATOR_CODE,
            Observation.country_id.in_(country_ids),
            Observation.period_start.in_(period_starts),
            Observation.status == ObservationStatus.ACTIVE,
            Observation.value_numeric.is_not(None),
            Observation.administrative_area_id.is_(None),
        )
```

- [ ] **Step 4: Run the guard test again**

```bash
REIM_TEST_DATABASE_URL=postgresql+psycopg://reim:reim@localhost:55432/reim \
  .venv/bin/pytest tests/integration/test_comparison_repository.py -k provincial -v
```

Expected: PASS.

- [ ] **Step 5: Run the full comparison suite, to confirm zero regression**

```bash
REIM_TEST_DATABASE_URL=postgresql+psycopg://reim:reim@localhost:55432/reim \
  .venv/bin/pytest tests/integration/test_comparison_repository.py tests/unit/test_comparison.py -v
```

Expected: every pre-existing test still passes — the spec's own claim (§4) is that this is a no-op
change for the entire existing catalog, since nothing before this plan ever set
`administrative_area_id`. If anything regresses, that claim was wrong; say so in the task report
rather than forcing the test to pass.

- [ ] **Step 6: Gate and commit**

```bash
.venv/bin/ruff check . && .venv/bin/ruff format --check . && .venv/bin/mypy reim apps
REIM_TEST_DATABASE_URL=postgresql+psycopg://reim:reim@localhost:55432/reim .venv/bin/pytest -q
```

```bash
git add reim/repositories/comparison.py tests/integration/test_comparison_repository.py
git commit -m "fix(compare): exclude subnational observations from cross-country comparison"
```

---

### Task 5: `/api/v1/observations` surface

**Files:**
- Modify: `reim/schemas/observations.py`
- Modify: `reim/repositories/observations.py`
- Modify: `apps/api/routers/observations.py`
- Test: `tests/integration/test_api.py` (existing — extend)

**Interfaces:**
- Consumes: `Observation.administrative_area` relationship (Task 1), a persisted provincial
  observation (Task 3).
- Produces: `ObservationRead.administrative_area_code: str | None`,
  `ObservationRead.administrative_area_name: str | None`. `ObservationFilters.administrative_area: str | None`.
  `GET /api/v1/observations?administrative_area=<code>`.

- [ ] **Step 1: Extend `ObservationRead`**

In `reim/schemas/observations.py`, add two flat fields — matching how `country_iso2`/`country_iso3`/
`country_name` are already flattened rather than nested, not the nested `{code, name}` shape an
earlier draft of the spec loosely suggested. After `currency_code` (currently line 41):

```text
    published_at: datetime | None
    retrieved_at: datetime
    source_url: str
    source_record_id: str | None
    content_hash: str

    administrative_area_code: str | None = Field(
        default=None, description="Publisher's code for a subnational area, e.g. a province."
    )
    administrative_area_name: str | None = Field(
        default=None, description="Human-readable name of the administrative area."
    )
```

(Placed after the provenance block and before `status`, so the two new fields don't disturb the
existing field order that `from_model`'s positional understanding — actually keyword — depends on;
since `from_model` builds with keyword arguments, ordering here is cosmetic, but keep it readable.)

Update `from_model` (currently lines 58-89) to populate them:

```text
    @classmethod
    def from_model(cls, observation: Observation) -> ObservationRead:
        """Flatten an ORM observation and its joined reference rows."""
        return cls(
            id=observation.id,
            country_iso2=observation.country.iso2,
            country_iso3=observation.country.iso3,
            country_name=observation.country.name,
            indicator_code=observation.indicator.code,
            indicator_name=observation.indicator.name,
            source_key=observation.source.source_key,
            period_start=observation.period_start,
            period_end=observation.period_end,
            period_label=observation.period_label,
            value_numeric=observation.value_numeric,
            value_text=observation.value_text,
            unit=observation.unit,
            currency_code=observation.currency_code,
            published_at=observation.published_at,
            retrieved_at=observation.retrieved_at,
            source_url=observation.source_url,
            source_record_id=observation.source_record_id,
            content_hash=observation.content_hash,
            administrative_area_code=(
                observation.administrative_area.code if observation.administrative_area else None
            ),
            administrative_area_name=(
                observation.administrative_area.name if observation.administrative_area else None
            ),
            status=observation.status,
            validation_status=observation.validation_status,
            revision_count=observation.revision_count,
            connector_version=observation.connector_version,
            pipeline_version=observation.pipeline_version,
            raw_metadata=observation.raw_metadata,
            created_at=observation.created_at,
            updated_at=observation.updated_at,
        )
```

- [ ] **Step 2: Add the filter**

In `reim/repositories/observations.py`, import `AdministrativeArea`:

```text
from reim.database.models import AdministrativeArea, Country, DataSource, Indicator, Observation
```

Add the field to `ObservationFilters` (currently lines 27-37):

```text
@dataclass(frozen=True, slots=True)
class ObservationFilters:
    """Filter set shared by the list, count and export queries."""

    country: str | None = None
    indicator: str | None = None
    source: str | None = None
    category: str | None = None
    administrative_area: str | None = None
    period_start_from: date | None = None
    period_start_to: date | None = None
    validation_status: ValidationStatus | None = None
    status: ObservationStatus | None = ObservationStatus.ACTIVE
```

`_base_query()` and `count_observations()` both need an outer join onto `AdministrativeArea` — an
**outer** join, not an inner one, since most rows have no administrative area at all and an inner
join would silently drop every national-level observation from every unfiltered query:

```text
def _base_query() -> Select[tuple[Observation]]:
    return (
        select(Observation)
        .join(Observation.country)
        .join(Observation.indicator)
        .join(Observation.source)
        .outerjoin(Observation.administrative_area)
    )
```

```text
def count_observations(session: Session, filters: ObservationFilters) -> int:
    """Return how many observations match ``filters``."""
    statement = (
        select(func.count(Observation.id))
        .select_from(Observation)
        .join(Observation.country)
        .join(Observation.indicator)
        .join(Observation.source)
        .outerjoin(Observation.administrative_area)
    )
    return int(session.scalar(apply_filters(statement, filters)) or 0)
```

Add the filter clause to `apply_filters` (currently lines 49-69), after the `source` clause:

```text
    if filters.source:
        statement = statement.where(DataSource.source_key == filters.source)
    if filters.administrative_area:
        statement = statement.where(AdministrativeArea.code == filters.administrative_area)
```

- [ ] **Step 3: Wire the query parameter**

In `apps/api/routers/observations.py`, add the parameter to `observation_filters()` (currently
lines 28-47):

```text
def observation_filters(
    country: Annotated[
        str | None,
        Query(min_length=2, max_length=3, description="ISO alpha-2 or alpha-3 country code."),
    ] = None,
    indicator: Annotated[str | None, Query(description="REIM indicator code.")] = None,
    source: Annotated[str | None, Query(description="Catalog source key.")] = None,
    category: Annotated[str | None, Query(description="Indicator category.")] = None,
    administrative_area: Annotated[
        str | None,
        Query(description="Publisher's code for a subnational area. Unset returns both national and subnational rows."),
    ] = None,
    date_from: Annotated[
        date | None, Query(description="Earliest period start, inclusive (YYYY-MM-DD).")
    ] = None,
    date_to: Annotated[
        date | None, Query(description="Latest period start, inclusive (YYYY-MM-DD).")
    ] = None,
    validation_status: Annotated[ValidationStatus | None, Query()] = None,
    status: Annotated[
        ObservationStatus | None,
        Query(description="Lifecycle status. Defaults to active observations only."),
    ] = ObservationStatus.ACTIVE,
) -> ObservationFilters:
    """Build and sanity-check the shared observation filter set."""
    if date_from and date_to and date_from > date_to:
        msg = f"date_from ({date_from}) must not be later than date_to ({date_to})"
        raise InvalidRequestError(msg, date_from=str(date_from), date_to=str(date_to))
    return ObservationFilters(
        country=country,
        indicator=indicator,
        source=source,
        category=category,
        administrative_area=administrative_area,
        period_start_from=date_from,
        period_start_to=date_to,
        validation_status=validation_status,
        status=status,
    )
```

- [ ] **Step 4: Write the failing API test**

`tests/integration/test_api.py`'s `client` fixture (`tests/integration/test_api.py:21-53`) is built
from `seeded_session` and pre-loads its own 15 Nicaragua-only observations before yielding — every
existing test in this file (e.g. `test_list_observations`, `tests/integration/test_api.py:201-203`)
reads the paginated response as `body["meta"]["total"]` and `body["data"]`, **not** `body["total"]`/
`body["items"]`. Requesting `seeded_session` as a second parameter alongside `client` gets the same
underlying session `client` already depends on (pytest caches a fixture per test), so writing more
observations to it after `client` is constructed, then committing, makes them visible to `client`'s
subsequent requests. Add, near the other `# Observations` tests:

```text
def test_observations_filters_by_administrative_area(
    client: TestClient, seeded_session: Session, make_observation
) -> None:  # type: ignore[no-untyped-def]
    from reim.services.observation_writer import write_observations

    write_observations(
        seeded_session,
        [
            make_observation(
                "2023", "205.1", country_iso3="PAN", indicator_code="ni_cpi_inflation_annual"
            ),
            make_observation(
                "2023",
                "9.6",
                country_iso3="PAN",
                indicator_code="ni_cpi_inflation_annual",
                administrative_area_code="01",
            ),
        ],
        connector_version="1.0.0",
    )
    seeded_session.commit()

    unfiltered = client.get(
        "/api/v1/observations", params={"country": "PA", "indicator": "ni_cpi_inflation_annual"}
    )
    assert unfiltered.status_code == 200
    assert unfiltered.json()["meta"]["total"] == 2

    filtered = client.get(
        "/api/v1/observations",
        params={
            "country": "PA",
            "indicator": "ni_cpi_inflation_annual",
            "administrative_area": "01",
        },
    )
    assert filtered.status_code == 200
    body = filtered.json()
    assert body["meta"]["total"] == 1
    assert body["data"][0]["administrative_area_code"] == "01"
    assert body["data"][0]["administrative_area_name"] == "Bocas del Toro"
```

- [ ] **Step 5: Run it to verify it fails, then implement, then verify it passes**

```bash
REIM_TEST_DATABASE_URL=postgresql+psycopg://reim:reim@localhost:55432/reim \
  .venv/bin/pytest tests/integration/test_api.py -k administrative_area -v
```

Expected first: FAIL (422 on the unknown `administrative_area` query param, or a `KeyError` on the
response body). After Steps 1-3: PASS.

- [ ] **Step 6: Run the full API test suite**

```bash
REIM_TEST_DATABASE_URL=postgresql+psycopg://reim:reim@localhost:55432/reim \
  .venv/bin/pytest tests/integration/test_api.py -v
```

Expected: every existing test still passes.

- [ ] **Step 7: Gate and commit**

```bash
.venv/bin/ruff check . && .venv/bin/ruff format --check . && .venv/bin/mypy reim apps
REIM_TEST_DATABASE_URL=postgresql+psycopg://reim:reim@localhost:55432/reim .venv/bin/pytest -q
```

```bash
git add reim/schemas/observations.py reim/repositories/observations.py \
  apps/api/routers/observations.py tests/integration/test_api.py
git commit -m "feat(api): expose and filter observations by administrative area"
```

---

### Task 6: The INEC Panama connector

**Files:**
- Create: `reim/ingestion/connectors/panama/__init__.py`
- Create: `reim/ingestion/connectors/panama/inec_provincial.py`
- Modify: `reim/domain/sources/organizations.py`
- Modify: `reim/domain/indicators/registry.py`
- Modify: `sources/catalog.yml`
- Modify: `sources/quality_rules.yml`
- Create: `tests/fixtures/inec_catalogue_excerpt.json`
- Create: `tests/fixtures/inec_choropleth_232.json`
- Create: `tests/fixtures/inec_choropleth_206.json`
- Create: `tests/fixtures/inec_choropleth_207.json`
- Modify: `tests/conftest.py` (fixture loaders)
- Test: `tests/unit/test_inec_provincial_connector.py` (new)

**Interfaces:**
- Consumes: everything from Tasks 1-3 (the full write path must exist for `reim db seed` and a real
  pipeline run to work end-to-end, though this task's own tests only exercise `transform()`, which
  needs no database).
- Produces: `INECProvincialConnector` (`connector_key = "inec_pa_provincial"`), three new indicator
  codes, one new catalog entry, one new organization (`INEC_PA`).

- [ ] **Step 1: Record the real fixtures**

These four requests were already run and verified reproducible (fetched twice, byte-identical) on
2026-09-20 while writing this plan's spec. Run them again now, to record what the implementation
actually ships against rather than trusting a transcript from earlier:

```bash
curl -s -A "REIM-fixture/1.0" \
  "https://www.inec.gob.pa/m_2/api/meta/estructura-completa" \
  -o /tmp/inec_full_catalogue.json
curl -s -A "REIM-fixture/1.0" \
  "https://www.inec.gob.pa/m_2/api/data/choropleth?nivel_geografico=Provincia&id_variable=232&anio=2023" \
  -o tests/fixtures/inec_choropleth_232.json
curl -s -A "REIM-fixture/1.0" \
  "https://www.inec.gob.pa/m_2/api/data/choropleth?nivel_geografico=Provincia&id_variable=206&anio=2023" \
  -o tests/fixtures/inec_choropleth_206.json
curl -s -A "REIM-fixture/1.0" \
  "https://www.inec.gob.pa/m_2/api/data/choropleth?nivel_geografico=Provincia&id_variable=207&anio=2023" \
  -o tests/fixtures/inec_choropleth_207.json
```

Expect each of the three `inec_choropleth_*.json` files to parse as a JSON list of 11 objects, 10
with `"is_total": false` and one with `"is_total": true`. Verify:

```bash
python3 -c "
import json
for id in (232, 206, 207):
    rows = json.load(open(f'tests/fixtures/inec_choropleth_{id}.json'))
    assert len(rows) == 11, (id, len(rows))
    assert sum(1 for r in rows if r['is_total']) == 1, id
    print(id, 'ok')
"
```

The full catalogue (`/tmp/inec_full_catalogue.json`) is 218 variables, ~94 KB — too large to check
in whole as a test fixture. Extract just the three variables this connector reads, keeping every
field a real API response would have (this is still a real recording, just trimmed to what's
relevant, matching how `tests/fixtures/sieca_filters.json` and its siblings are real-but-scoped
recordings rather than the full multi-country payload SIECA's own tooling might return in some
other configuration):

```bash
python3 -c "
import json

data = json.load(open('/tmp/inec_full_catalogue.json'))
wanted = {232, 206, 207}
found = []
for tema in data['data']:
    for cat in tema.get('categorias', []):
        for v in cat.get('variables', []):
            if v['id'] in wanted:
                found.append(v)

assert len(found) == 3, f'expected 3 variables, found {len(found)}'
with open('tests/fixtures/inec_catalogue_excerpt.json', 'w', encoding='utf-8') as f:
    json.dump(found, f, ensure_ascii=False, indent=2)
print('wrote', len(found), 'variable definitions')
"
```

Read `tests/fixtures/inec_catalogue_excerpt.json` afterward and confirm by eye that all three
entries carry `"anio_referencia": 2023`, `"frecuencia_actualizacion": "Anual"`, and
`"nivel_geografico_minimo": "Provincia"` — these are the facts the connector's year-discovery step
depends on, and this plan's design rests on them being true today.

- [ ] **Step 2: Register the fixtures in `conftest.py`**

In `tests/conftest.py`, following the `sieca_*_json` fixtures' exact shape (currently around lines
109-131):

```text
@pytest.fixture(scope="session")
def inec_catalogue_excerpt_json() -> str:
    """Real INEC Panama catalogue entries for the three variables this connector reads."""
    return (FIXTURES / "inec_catalogue_excerpt.json").read_text(encoding="utf-8")


@pytest.fixture(scope="session")
def inec_choropleth_232_json() -> str:
    """Real INEC choropleth response: automobiles per 1000 inhabitants, by province, 2023."""
    return (FIXTURES / "inec_choropleth_232.json").read_text(encoding="utf-8")


@pytest.fixture(scope="session")
def inec_choropleth_206_json() -> str:
    """Real INEC choropleth response: residential building count, by province, 2023."""
    return (FIXTURES / "inec_choropleth_206.json").read_text(encoding="utf-8")


@pytest.fixture(scope="session")
def inec_choropleth_207_json() -> str:
    """Real INEC choropleth response: non-residential building count, by province, 2023."""
    return (FIXTURES / "inec_choropleth_207.json").read_text(encoding="utf-8")
```

- [ ] **Step 3: Register the organization**

In `reim/domain/sources/organizations.py`, add (alongside INIDE, matching its exact shape — find
INIDE's entry first, currently around line 44-51, and add this one in the same Panama-adjacent
region of the tuple, or at the end if the file isn't grouped by country strictly):

```text
    OrganizationDefinition(
        code="INEC_PA",
        name="Instituto Nacional de Estadística y Censo",
        short_name="INEC",
        organization_type=OrganizationType.STATISTICS_OFFICE,
        website_url="https://www.inec.gob.pa",
        country_iso2="PA",
    ),
```

- [ ] **Step 4: Register the three indicators**

In `reim/domain/indicators/registry.py`, add three entries, following the existing shape exactly
(check the real `IndicatorCategory`/`ValueType` import already at the top of the file):

```text
    IndicatorDefinition(
        code="pa_automobiles_per_1000_provincial_annual",
        name="Panama — automobiles in circulation per 1,000 inhabitants, by province",
        description=(
            "Registered automobiles per 1,000 inhabitants, by province and nationally, from "
            "INEC's Panamá en Cifras Digital. Sourced from municipal treasury plate-sale records."
        ),
        category=IndicatorCategory.REAL_SECTOR,
        frequency=Frequency.ANNUAL,
        unit="automóviles",
        value_type=ValueType.RATIO,
        methodology_url="https://www.inec.gob.pa",
    ),
    IndicatorDefinition(
        code="pa_residential_buildings_count_provincial_annual",
        name="Panama — count of residential buildings, by province",
        description=(
            "Count of residential building permits/constructions, by province and nationally, "
            "from INEC's Panamá en Cifras Digital."
        ),
        category=IndicatorCategory.REAL_SECTOR,
        frequency=Frequency.ANNUAL,
        unit="unidades",
        value_type=ValueType.LEVEL,
        methodology_url="https://www.inec.gob.pa",
    ),
    IndicatorDefinition(
        code="pa_nonresidential_buildings_count_provincial_annual",
        name="Panama — count of non-residential buildings, by province",
        description=(
            "Count of non-residential building permits/constructions, by province and "
            "nationally, from INEC's Panamá en Cifras Digital."
        ),
        category=IndicatorCategory.REAL_SECTOR,
        frequency=Frequency.ANNUAL,
        unit="unidades",
        value_type=ValueType.LEVEL,
        methodology_url="https://www.inec.gob.pa",
    ),
```

- [ ] **Step 5: Add the catalog entry**

In `sources/catalog.yml`, add a new entry (matching an existing entry's exact key ordering and
comment-block style — copy the header-comment convention from the nearest existing entry):

```yaml
  # ------------------------------------------------------------------------
  # Panama — INEC provincial economic indicators
  #
  # Open, keyless JSON API recovered from the "Panamá en Cifras Digital" map
  # application's own bundle (docs/sources.md). REIM's first subnational
  # data: provincial-level figures, plus each response's own national total.
  # ------------------------------------------------------------------------
  - key: inec_pa_provincial
    name: Panama provincial economic indicators (INEC)
    description: >-
      Provincial-level automobile registration and building-count figures
      from INEC's Panamá en Cifras Digital, plus each variable's national
      total. REIM's first subnational data.
    country: PA
    organization: INEC_PA
    category: real_sector
    access_type: http_api
    frequency: annual
    format: json
    base_url: https://www.inec.gob.pa/m_2/api
    documentation_url: https://www.inec.gob.pa
    connector: reim.ingestion.connectors.panama.inec_provincial
    indicators:
      - pa_automobiles_per_1000_provincial_annual
      - pa_residential_buildings_count_provincial_annual
      - pa_nonresidential_buildings_count_provincial_annual
    license: public_official_data
    official: true
    enabled: true
```

- [ ] **Step 6: Add the freshness thresholds**

In `sources/quality_rules.yml`, under `indicators:`, add three entries:

```yaml
  # Panama — INEC provincial indicators -----------------------------------
  # freshness_max_age_days is set loose (800, this file's widest precedent)
  # because INEC's own declared cadence is "Anual" but the actual
  # republication interval is unconfirmed: only one reference year has ever
  # been observed for any of these three variables.
  pa_automobiles_per_1000_provincial_annual:
    min_value: 0
    max_value: 2000
    allow_negative: false
    allow_zero: true
    freshness_max_age_days: 800

  pa_residential_buildings_count_provincial_annual:
    min_value: 0
    max_value: null
    allow_negative: false
    allow_zero: true
    freshness_max_age_days: 800

  pa_nonresidential_buildings_count_provincial_annual:
    min_value: 0
    max_value: null
    allow_negative: false
    allow_zero: true
    freshness_max_age_days: 800
```

- [ ] **Step 7: Write the failing connector tests**

Create `reim/ingestion/connectors/panama/__init__.py` (empty).

Create `tests/unit/test_inec_provincial_connector.py`:

```text
"""Unit tests for the INEC Panama provincial connector.

Every payload replayed here is a real recording; see tests/fixtures/.
"""

from __future__ import annotations

import json
from datetime import UTC, datetime
from decimal import Decimal

import pytest

from reim.domain.pipelines.models import NormalizedObservation, RawDataset
from reim.domain.sources.catalog import load_catalog
from reim.ingestion.connectors.panama.inec_provincial import INECProvincialConnector
from tests.conftest import REPO_ROOT

VARIABLE_IDS = (232, 206, 207)
INDICATOR_CODES = {
    232: "pa_automobiles_per_1000_provincial_annual",
    206: "pa_residential_buildings_count_provincial_annual",
    207: "pa_nonresidential_buildings_count_provincial_annual",
}


def build_connector() -> INECProvincialConnector:
    catalog = load_catalog(REPO_ROOT / "sources" / "catalog.yml")
    return INECProvincialConnector(catalog.get("inec_pa_provincial"))


def build_raw(
    catalogue: str, choropleth_232: str, choropleth_206: str, choropleth_207: str
) -> RawDataset:
    return RawDataset(
        source_key="inec_pa_provincial",
        retrieved_at=datetime(2026, 9, 20, 12, 0, tzinfo=UTC),
        source_url="https://www.inec.gob.pa/m_2/api/data/choropleth",
        payload={
            "catalogue": json.loads(catalogue),
            "choropleth": {
                232: json.loads(choropleth_232),
                206: json.loads(choropleth_206),
                207: json.loads(choropleth_207),
            },
        },
        content_type="application/json",
        http_status=200,
        metadata={"operation": "choropleth"},
    )


@pytest.fixture
def raw(
    inec_catalogue_excerpt_json: str,
    inec_choropleth_232_json: str,
    inec_choropleth_206_json: str,
    inec_choropleth_207_json: str,
) -> RawDataset:
    return build_raw(
        inec_catalogue_excerpt_json,
        inec_choropleth_232_json,
        inec_choropleth_206_json,
        inec_choropleth_207_json,
    )


def test_each_variable_yields_eleven_observations(raw: RawDataset) -> None:
    observations = build_connector().transform(raw)

    by_indicator: dict[str, int] = {}
    for obs in observations:
        by_indicator[obs.indicator_code] = by_indicator.get(obs.indicator_code, 0) + 1
    assert by_indicator == {code: 11 for code in INDICATOR_CODES.values()}
    assert len(observations) == 33


def test_exactly_one_national_observation_per_indicator(raw: RawDataset) -> None:
    observations = build_connector().transform(raw)

    for code in INDICATOR_CODES.values():
        national = [
            o
            for o in observations
            if o.indicator_code == code and o.administrative_area_code is None
        ]
        assert len(national) == 1


def test_exactly_ten_provincial_observations_per_indicator_with_no_duplicates(
    raw: RawDataset,
) -> None:
    observations = build_connector().transform(raw)

    for code in INDICATOR_CODES.values():
        codes = [
            o.administrative_area_code
            for o in observations
            if o.indicator_code == code and o.administrative_area_code is not None
        ]
        assert len(codes) == 10
        assert len(set(codes)) == 10


def test_every_observation_is_panama_2023(raw: RawDataset) -> None:
    observations = build_connector().transform(raw)

    for obs in observations:
        assert obs.country_iso3 == "PAN"
        assert obs.period.label == "2023"


def test_the_year_comes_from_the_catalogue_not_a_constant(raw: RawDataset) -> None:
    """If the catalogue excerpt is edited to claim a different year, transform() must follow it.

    Guards against the year being silently hardcoded to 2023 in the connector
    despite the design requiring it to be read from anio_referencia.
    """
    catalogue = json.loads(
        (REPO_ROOT / "tests" / "fixtures" / "inec_catalogue_excerpt.json").read_text(
            encoding="utf-8"
        )
    )
    for entry in catalogue:
        entry["anio_referencia"] = 2030

    raw_with_future_year = RawDataset(
        source_key="inec_pa_provincial",
        retrieved_at=datetime(2026, 9, 20, 12, 0, tzinfo=UTC),
        source_url="https://www.inec.gob.pa/m_2/api/data/choropleth",
        payload={
            "catalogue": catalogue,
            "choropleth": {
                232: json.loads(
                    (REPO_ROOT / "tests" / "fixtures" / "inec_choropleth_232.json").read_text(
                        encoding="utf-8"
                    )
                ),
                206: json.loads(
                    (REPO_ROOT / "tests" / "fixtures" / "inec_choropleth_206.json").read_text(
                        encoding="utf-8"
                    )
                ),
                207: json.loads(
                    (REPO_ROOT / "tests" / "fixtures" / "inec_choropleth_207.json").read_text(
                        encoding="utf-8"
                    )
                ),
            },
        },
        content_type="application/json",
        http_status=200,
        metadata={},
    )
    observations = build_connector().transform(raw_with_future_year)
    assert all(o.period.label == "2030" for o in observations)


def test_the_national_row_carries_the_choropleths_own_total(raw: RawDataset) -> None:
    """The is_total row's value, not a sum computed from the ten provinces."""
    observations = build_connector().transform(raw)
    national = next(
        o
        for o in observations
        if o.indicator_code == "pa_automobiles_per_1000_provincial_annual"
        and o.administrative_area_code is None
    )
    provincial_total = sum(
        Decimal(str(o.value_numeric))
        for o in observations
        if o.indicator_code == "pa_automobiles_per_1000_provincial_annual"
        and o.administrative_area_code is not None
    )
    # The national figure is a rate (per 1,000 inhabitants), not a sum of the
    # provincial rates, so it must NOT equal the naive sum of the ten — this
    # pins that the connector reads is_total rather than computing one.
    assert national.value_numeric != provincial_total


def test_unit_comes_from_the_response_not_reinvented(raw: RawDataset) -> None:
    observations = build_connector().transform(raw)
    automobiles = next(
        o for o in observations if o.indicator_code == "pa_automobiles_per_1000_provincial_annual"
    )
    assert automobiles.unit == "automóviles"
```

Read `tests/fixtures/inec_choropleth_232.json` after Step 1 and confirm the exact `unidad_medida`
string it carries before trusting `"automóviles"` in the last test above — use whatever the real
fixture actually says.

- [ ] **Step 8: Run them to verify they fail**

```bash
.venv/bin/pytest tests/unit/test_inec_provincial_connector.py -v
```

Expected: collection error or `ModuleNotFoundError` — `INECProvincialConnector` doesn't exist yet.

- [ ] **Step 9: Write the connector**

Create `reim/ingestion/connectors/panama/inec_provincial.py`. This codebase does not call `httpx`
directly from a connector — every connector goes through the shared helpers in
`reim/ingestion/http.py` (`http_client` for a configured, retrying client; `fetch` for a GET with
the shared retry/backoff policy; `ensure_ok` to validate status and content type before a response
reaches `transform`), confirmed against `reim/ingestion/connectors/regional/sieca_services_trade.py:124-150`
while writing this plan. The code below already uses them:

```text
"""INEC Panama — provincial economic indicators.

REIM's first subnational data. Three variables from INEC's "Panamá en
Cifras Digital" choropleth API, each yielding one national observation
(the response's own ``is_total`` row) and ten provincial ones.

See docs/sources.md's "INEC Panama" entry and
docs/superpowers/specs/2026-09-20-inec-panama-provincial-design.md for the
research this is built from.
"""

from __future__ import annotations

from decimal import Decimal
from typing import Any

from reim.core.constants import CheckSeverity, CheckType, Frequency
from reim.core.exceptions import ExtractionError, TransformationError
from reim.domain.observations.periods import parse_period
from reim.domain.pipelines.models import NormalizedObservation, QualityResult, RawDataset
from reim.ingestion.base import BaseConnector
from reim.ingestion.http import ensure_ok, fetch, http_client

#: Variable id -> REIM indicator code. Order doesn't matter; this is the
#: authority on which three of INEC's 218 catalogue entries this connector
#: reads.
VARIABLE_INDICATORS: dict[int, str] = {
    232: "pa_automobiles_per_1000_provincial_annual",
    206: "pa_residential_buildings_count_provincial_annual",
    207: "pa_nonresidential_buildings_count_provincial_annual",
}


class INECProvincialConnector(BaseConnector):
    """Provincial-level figures from INEC Panama, plus each variable's national total."""

    connector_key = "inec_pa_provincial"
    version = "1.0.0"
    expected_frequency = Frequency.ANNUAL

    async def extract(self) -> RawDataset:
        """Fetch the catalogue, then one choropleth payload per variable.

        Four requests. The year comes from the catalogue's own
        ``anio_referencia`` rather than a constant, so a future INEC
        republication is picked up without a code change — the same
        principle ``sieca_services_trade.py``'s ``extract()`` applies to its
        quarter window.

        Raises:
            ExtractionError: The service was unreachable, kept failing, or
                answered with something other than JSON.
        """
        base = str(self.source.base_url).rstrip("/")
        retrieved_at = self.now()

        async with http_client(user_agent=self.source.user_agent) as client:
            catalogue_response = await fetch(client, f"{base}/meta/estructura-completa")
            ensure_ok(catalogue_response, expected_content_type="json")
            catalogue = _flatten_catalogue(catalogue_response.json())
            years = {
                variable_id: _year_for(catalogue, variable_id)
                for variable_id in VARIABLE_INDICATORS
            }

            choropleths: dict[int, list[dict[str, Any]]] = {}
            for variable_id, year in years.items():
                response = await fetch(
                    client,
                    f"{base}/data/choropleth",
                    params={
                        "nivel_geografico": "Provincia",
                        "id_variable": variable_id,
                        "anio": year,
                    },
                )
                ensure_ok(response, expected_content_type="json")
                choropleths[variable_id] = response.json()

        return RawDataset(
            source_key=self.connector_key,
            retrieved_at=retrieved_at,
            source_url=f"{base}/data/choropleth",
            payload={
                "catalogue": [entry for entry in catalogue if entry["id"] in VARIABLE_INDICATORS],
                "choropleth": choropleths,
            },
            content_type="application/json",
            http_status=200,
            metadata={"variable_ids": list(VARIABLE_INDICATORS)},
        )

    def transform(self, raw: RawDataset) -> list[NormalizedObservation]:
        payload = raw.payload
        catalogue_by_id = {entry["id"]: entry for entry in payload["catalogue"]}
        observations: list[NormalizedObservation] = []

        for variable_id, indicator_code in VARIABLE_INDICATORS.items():
            rows = payload["choropleth"][variable_id]
            year = catalogue_by_id[variable_id]["anio_referencia"]
            period = parse_period(str(year), Frequency.ANNUAL)

            for row in rows:
                value = row.get("valor")
                if value is None:
                    continue
                observations.append(
                    NormalizedObservation(
                        country_iso3="PAN",
                        indicator_code=indicator_code,
                        source_key=self.connector_key,
                        period=period,
                        unit=row["unidad_medida"],
                        retrieved_at=raw.retrieved_at,
                        source_url=raw.source_url,
                        value_numeric=Decimal(str(value).strip()),
                        source_record_id=f"{variable_id}:{row.get('id_provincia') or 'total'}",
                        administrative_area_code=(
                            None if row.get("is_total") else row.get("id_provincia")
                        ),
                        raw_metadata={
                            "inec_variable_id": variable_id,
                            "is_total": row.get("is_total", False),
                        },
                    )
                )

        if not observations:
            msg = "INEC provincial connector produced zero observations"
            raise TransformationError(msg)

        return observations

    def validate(self, observations: list[NormalizedObservation]) -> list[QualityResult]:
        """Assert each variable produced exactly 11 rows: 10 provinces + 1 national."""
        return [self._check_eleven_rows_per_variable(observations)]

    def _check_eleven_rows_per_variable(
        self, observations: list[NormalizedObservation]
    ) -> QualityResult:
        """All 11 rows (10 provinces + 1 national) must appear per variable.

        Follows the same idiom as ``sieca_services_trade.py``'s
        ``_check_six_countries`` — a short count means a broken request, not
        a soft data-quality issue, so this fails at ``CRITICAL`` like that
        check does, not ``ERROR``.
        """
        counts: dict[str, int] = {}
        for obs in observations:
            counts[obs.indicator_code] = counts.get(obs.indicator_code, 0) + 1

        short = {code: count for code, count in counts.items() if count != 11}
        if not short:
            return QualityResult.passed(
                "inec_eleven_rows_per_variable",
                CheckType.COMPLETENESS,
                "every variable produced 10 provincial rows and 1 national row",
                expected_value="11",
                actual_value="11",
            )
        return QualityResult.failure(
            "inec_eleven_rows_per_variable",
            CheckType.COMPLETENESS,
            CheckSeverity.CRITICAL,
            f"expected 11 observations (10 provinces + 1 national) per variable, got {short}",
            expected_value="11",
            actual_value=str(short),
        )


def _flatten_catalogue(payload: dict[str, Any]) -> list[dict[str, Any]]:
    """Flatten INEC's theme -> category -> variables nesting into one list."""
    flat: list[dict[str, Any]] = []
    for theme in payload["data"]:
        for category in theme.get("categorias", []):
            flat.extend(category.get("variables", []))
    return flat


def _year_for(catalogue: list[dict[str, Any]], variable_id: int) -> int:
    for entry in catalogue:
        if entry["id"] == variable_id:
            year: int = entry["anio_referencia"]
            return year
    msg = f"Variable {variable_id} not found in INEC's catalogue"
    raise ExtractionError(msg)
```

`QualityResult.passed`/`.failure` (`reim/domain/pipelines/models.py:119-155`) are this codebase's
established idiom — confirmed against `sieca_services_trade.py`'s `_check_six_countries`
(`reim/ingestion/connectors/regional/sieca_services_trade.py:316-336`), which the code above
matches exactly, including using `CRITICAL` for the same reason: a short row count means the
request itself was broken, not a soft data-quality issue an `ERROR`-level per-observation rejection
would fit.

**On `raw_metadata`, above:** a real response row was checked while writing this plan
(`tests/fixtures/inec_choropleth_232.json`'s first row, `id_provincia: "01"`) — there is **no**
field carrying a province's own name anywhere in the payload. `nombre` and `definicion` both just
repeat the variable's own title on every row, provincial or national, so falling back to either
would silently store the wrong thing as if it were a province name. `raw_metadata` above therefore
carries only `inec_variable_id` and `is_total` — genuinely present fields. The province's name is
never lost: it comes from the seeded `AdministrativeArea` row the observation's
`administrative_area_id` FK resolves to (Task 3), which is where `ObservationRead.administrative_area_name`
(Task 5) reads it from — not from anything INEC's own response carries.

- [ ] **Step 10: Run the connector tests**

```bash
.venv/bin/pytest tests/unit/test_inec_provincial_connector.py -v
```

Expected: all pass. If `test_the_national_row_carries_the_choropleths_own_total` fails because the
national and summed values happen to be numerically close, that test's premise (a rate is not a
sum) should still hold — investigate rather than deleting the assertion.

- [ ] **Step 11: Confirm the catalog and quality-rules changes validate**

```bash
make catalog-validate
```

(`Makefile:134-136` — runs `reim.cli catalog validate` over both `sources/catalog.yml` and
`sources/quality_rules.yml`; it is also what `make check`, the project's own top-level gate, calls.)

Expected: no exception, output confirming the catalog and quality rules are valid.

- [ ] **Step 12: Seed and confirm an end-to-end run works against the real API**

```bash
.venv/bin/reim db seed
.venv/bin/reim pipeline run inec_pa_provincial
```

Expected: the run succeeds, reports 33 observations (or close to it — `validate()`'s completeness
check should be green), and `reim db seed` reports the new organization, three indicators, and (if
Task 3 wasn't already run against this database) ten administrative areas created. This is the one
step in this task that hits the real network and the real database rather than a fixture — run it
once to confirm the whole path works, and note the actual counts in your task report.

- [ ] **Step 13: Gate and commit**

```bash
.venv/bin/ruff check . && .venv/bin/ruff format --check . && .venv/bin/mypy reim apps
REIM_TEST_DATABASE_URL=postgresql+psycopg://reim:reim@localhost:55432/reim .venv/bin/pytest -q
```

```bash
git add reim/ingestion/connectors/panama reim/domain/sources/organizations.py \
  reim/domain/indicators/registry.py sources/catalog.yml sources/quality_rules.yml \
  tests/fixtures/inec_*.json tests/conftest.py tests/unit/test_inec_provincial_connector.py
git commit -m "feat(panama): ingest INEC's provincial economic indicators"
```

---

### Task 7: Documentation and the whole-suite gate

**Files:**
- Modify: `docs/sources.md`
- Modify: `ROADMAP.md`

**Interfaces:** none — documentation only, plus the final full-repo gate.

- [ ] **Step 1: Update `docs/sources.md`'s INEC Panama section**

Find the section (`### INEC Panama — an open API behind a map, with one year of data`). Update its
`| **Status** |` row from "⚠️ Reachable and open, not ingested" to "✅ Enabled — 33 observations
[measured 2026-09-20 or whatever date you actually run Task 6's Step 12]", matching the format
every other enabled source in this file uses (check `docs/sources.md`'s SIECA section, `|
**Status** | ✅ **Enabled** — 1,242 observations, measured 2026-08-09 |`, for the exact style).

Rewrite the "Why REIM does not ingest it: one year, not a series" subsection (§ heading and all) —
it's no longer true. Replace with what actually shipped: which three variables, why those three
(genuinely `Anual`, not `Decenal`, no GDP overlap), and explicitly name the excluded ones
(`Empresas según naturaleza jurídica`, `Gastos`/`Ingresos de los municipios`, id 202-205) with the
same "measured but not stored, here is why" treatment this file gives every other excluded item —
match the tone and structure of the "Registered but not yet implemented" table or the SIECA
section's own "what the roadmap wanted was not what exists" honesty, whichever fits better once you
read both.

- [ ] **Step 2: Update `ROADMAP.md`**

In the `## v0.6.0 — Context` section, update the **Geospatial data** bullet. It currently reads (in
part) "One route is already measured: INEC Panama...". Mark it done, following this document's own
`~~struck-through~~ ✅ **done**` convention used throughout `v0.2.0`/`v0.3.0`/`v0.4.0`/`v0.5.0` —
read one of those entries first (e.g. the SIECA bullet) to match its exact register: what shipped,
the real numbers (33 observations, 3 variables, 10 provinces), and what's explicitly still open
(district level, the `Decenal` variables, any other country). Don't mark the whole v0.6.0 phase
done — **Economic news ingestion** and **Event correlation** are untouched by this plan.

- [ ] **Step 3: Full gate, one more time, from a clean check**

```bash
.venv/bin/ruff check .
.venv/bin/ruff format --check .
.venv/bin/mypy reim apps
.venv/bin/alembic upgrade head
.venv/bin/alembic check
REIM_TEST_DATABASE_URL=postgresql+psycopg://reim:reim@localhost:55432/reim .venv/bin/pytest -q
```

Expected: everything clean, and the full suite's passed count has grown by roughly 30-40 tests over
this plan's own additions (Task 1: 3, Task 2: 6, Task 3: 6, Task 4: 1, Task 5: 1, Task 6: 7 — plus
whatever `tests/fixtures/README.md`, if one exists and is meant to be updated per source, needs;
check for it and update it if the SIECA/CEPALSTAT fixtures are listed there).

- [ ] **Step 4: Commit**

```bash
git add docs/sources.md ROADMAP.md
git commit -m "docs: record INEC Panama's provincial data as shipped"
```

## Deliberately not in this plan

* **District (`Corregimiento`) level.** `AdministrativeArea.level` carries it without a schema
  change, but no district data is ingested here — see spec §8.
* **The `Decenal` INEC variables** (business counts, municipal revenue/spending). Recorded as
  measured-but-excluded in Task 7's `docs/sources.md` update.
* **Construction area/value** (id 202-205). Same shape as the three chosen variables; a reasonable
  second increment, not this one.
* **Any subnational source besides INEC Panama.** No second one is known.
* **Web dashboard or catalog-browser visualization** of provincial data (e.g. a choropleth). A
  later increment's own design.
* **`Economic news ingestion` and `Event correlation`**, v0.6.0's other two bullets. Untouched.

## Done when

* `alembic upgrade head` and `alembic check` both succeed with the new `administrative_areas` table
  and the extended `observations` natural key.
* `reim db seed` creates exactly 10 `AdministrativeArea` rows (Panama's provinces) and is idempotent
  on a second run.
* A national and a provincial observation for the same indicator and period persist as two distinct
  rows; a duplicate provincial observation does not double-insert; an unregistered province code
  raises `UnknownReferenceError` rather than silently creating a new area.
* `/api/v1/compare` is unaffected by the presence of provincial observations — verified by a guard
  test that plants one and confirms the comparison result is unchanged.
* `/api/v1/observations` can filter by `administrative_area` and returns both national and
  provincial rows by default.
* `reim pipeline run inec_pa_provincial` succeeds against the real INEC API and produces 33
  observations (11 rows × 3 variables): 3 national, 30 provincial.
* `docs/sources.md` and `ROADMAP.md` both reflect what actually shipped, not what was planned.
* The full gate passes: `ruff check`, `ruff format --check`, `mypy reim apps`, `alembic check`,
  `pytest -q`.
